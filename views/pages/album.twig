{{ include('partials/header.twig') }}

<div id="album">
    <h2>{{ album.name }}</h2>
    <p>{{ album.year }}</p>
    
    <div class="boxAlbum">
        <img src="../assets/img/{{ album.picture }}" alt="" width="300">

        <p>{{ album.songs|join(", ") }}</p>
    </div>
</div>

{{ include('partials/footer.twig') }}